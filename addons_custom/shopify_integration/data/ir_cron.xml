<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Product sync — every 60 minutes -->
    <record id="ir_cron_shopify_product_sync" model="ir.cron">
        <field name="name">Shopify: Sync Products</field>
        <field name="model_id" ref="model_shopify_config"/>
        <field name="state">code</field>
        <field name="code">model.action_sync_products()</field>
        <field name="interval_number">60</field>
        <field name="interval_type">minutes</field>
        <field name="active">True</field>
    </record>

    <!-- Order import — every 15 minutes -->
    <record id="ir_cron_shopify_order_import" model="ir.cron">
        <field name="name">Shopify: Import Orders</field>
        <field name="model_id" ref="model_shopify_config"/>
        <field name="state">code</field>
        <field name="code">model.action_sync_orders()</field>
        <field name="interval_number">15</field>
        <field name="interval_type">minutes</field>
        <field name="active">True</field>
    </record>
</odoo>
